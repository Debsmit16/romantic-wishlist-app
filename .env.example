# Romantic Wishlist App - Environment Variables Template
# Copy this file to .env.local and fill in your values

# App Configuration
VITE_APP_NAME="Romantic Wishlist"
VITE_APP_VERSION="1.0.0"

# Optional: Analytics (if you want to add Google Analytics later)
# VITE_GA_TRACKING_ID=your_google_analytics_id

# Optional: Custom API endpoints (for future features)
# VITE_API_BASE_URL=https://your-api.com

# Note: All environment variables must be prefixed with VITE_ to be accessible in the browser
# This app currently works entirely client-side with localStorage, so no backend variables are needed
